import { BarLoader } from "react-spinners"

const LoaderComponent = () => {
    return(
        <div className="loader">
            <BarLoader color="#7B948E" />
        </div>
        
    )
}

export default LoaderComponent